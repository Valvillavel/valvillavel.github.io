import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rinspector.newwebui',
  appName: 'newwebui',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
